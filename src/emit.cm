(* $Id: emit.cm,v 1.5 1999/09/06 15:59:28 nr Exp $ *)
(* module emit *)
(* code emission *)
group is
  $/basis.cm
  $/smlnj-lib.cm

  code.cm
  unparse.cm
  pp.cm
  unparse-pp.cm
  decoder.cm
  (* note error NOT imported --- should never get user error here *)

  emit-c.nw
  emit-ml.nw
  xform-c.nw
  control.nw (ccontrol.sml)
  control.nw (mlcontrol.sml)
  c-code.nw
  mangle.nw
